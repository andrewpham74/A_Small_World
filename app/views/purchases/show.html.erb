<section id="content-receipt" class="clearfix">
  <div id="receipt-container">
    <div class="dotmatrix left"></div>
    <div id="receipt-content">
      <h4 id="receipt-title">Thank You!</h4>
      <p><strong>RECEIPT</strong></p>
      <div id="receipt-details" class="row clearfix">
        <div class="col-sm-8 receipt-left">
          <p><strong class="font-weight-heavy"><%= @product.name %></strong><br/> <span class="font-weight-light"><%= @product.description %></span></p>
        </div>
        <div class="col-sm-4">
          <%= number_to_currency @product.price_in_cents / 100 %>
        </div>
      </div>
       <footer id="receipt-footer">
        <small>Your purchase is now complete. An email confirmation has been sent to your email address. We hope you enjoy it!</small>
      </footer>
      <br>

      <a href="<%= @product.download_url %>"><button type="button" class="btn btn-success btn-xs">DOWNLOAD VIDEO</button></a>
     
    </div>
    <div class="dotmatrix right"></div>
  </div>
  <div id="receipt-shadow"></div>
</section>
